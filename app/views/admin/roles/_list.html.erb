<table>
  <tr>
    <th>ID</th>
    <th>役割名</th>
    <th>初期<%= show_stars %>の数</th>
  </tr>
  <% @roles.each do |r| %>
    <tr>
      <td><%= r.id %></td>
      <td><%= r.name %></td>
      <td><%= show_stars(:number => r.default_stars) %></td>
    </tr>
  <% end %>
</table>
